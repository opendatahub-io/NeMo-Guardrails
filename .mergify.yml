pull_request_rules:
  - name: Backport merged PRs from develop → stable (skip .tekton-only changes)
    conditions:
      - base=develop
      - merged
      - author!=mergify[bot]
      - repository-full-name=opendatahub-io/NeMo-Guardrails
      - files~=^(?!\.tekton/).+
    actions:
      backport:
        branches:
          - stable
        ignore_conflicts: true
        labels:
          - bot/sync-stable
        title: "[stable] {{ title }}"
        body: |
          Backport of #{{ number }} — {{ title }}

          This PR cherry-picks the changes from `develop` into `stable`.
